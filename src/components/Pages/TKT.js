import React from "react";
import TKTCards from "../UI/Cards/TKTCard";

const Contact = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <TKTCards />
    </div>
  );
};

export default Contact;
